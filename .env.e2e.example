# Copy to .env.e2e and fill in your values
# Used for running e2e tests against a live Home Assistant instance with --live flag

# Home Assistant URL (include port if not 80/443)
HA_LIVE_URL=http://homeassistant.local:8123

# Long-lived access token from Home Assistant
# Create one at: Profile -> Security -> Long-Lived Access Tokens -> Create Token
HA_LIVE_TOKEN=your_long_lived_access_token_here
